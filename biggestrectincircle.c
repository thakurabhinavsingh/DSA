#include <stdio.h>
#include <math.h>
int main() {
    float radius=5.3, area;
    area = 2*radius*radius;
    printf("The area of the largest rectangle that can be inscribed in the circle is: %.2f\n", area);
    return 0;
}